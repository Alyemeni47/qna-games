<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>النتائج - لعبة سؤال وجواب</title>
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/game.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;900&display=swap" rel="stylesheet">
    <style>
        .results-container {
            max-width: 1000px;
            margin: 50px auto;
            padding: 40px;
            background-color: white;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            text-align: center;
        }
        
        .results-title {
            color: var(--primary);
            margin-bottom: 30px;
            font-size: 2.5rem;
        }
        
        .winner-section {
            background: linear-gradient(to right, #ffd700, #ffec8b);
            padding: 30px;
            border-radius: var(--border-radius);
            margin-bottom: 40px;
            position: relative;
            overflow: hidden;
        }
        
        .winner-section::before {
            content: "🏆";
            position: absolute;
            font-size: 10rem;
            opacity: 0.2;
            top: -40px;
            right: -40px;
        }
        
        .winner-title {
            color: #b8860b;
            font-size: 2rem;
            margin-bottom: 15px;
        }
        
        .winner-name {
            font-size: 3rem;
            font-weight: 900;
            color: var(--dark);
            margin-bottom: 10px;
        }
        
        .winner-score {
            font-size: 2.5rem;
            color: var(--primary);
            font-weight: 700;
        }
        
        .all-results {
            margin-bottom: 40px;
        }
        
        .results-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        
        .results-table th {
            background-color: var(--primary);
            color: white;
            padding: 15px;
            font-size: 1.2rem;
        }
        
        .results-table td {
            padding: 15px;
            border-bottom: 1px solid var(--light-gray);
            font-size: 1.1rem;
        }
        
        .results-table tr:nth-child(even) {
            background-color: var(--light-gray);
        }
        
        .results-table .position {
            font-weight: 700;
            font-size: 1.3rem;
            color: var(--primary);
        }
        
        .results-actions {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 40px;
        }
        
        .results-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(to right, var(--primary), var(--primary-dark));
            color: white;
            text-decoration: none;
            padding: 15px 35px;
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: 600;
            box-shadow: var(--shadow);
            transition: var(--transition);
            border: none;
            cursor: pointer;
        }
        
        .results-btn i {
            margin-left: 10px;
        }
        
        .results-btn:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-hover);
        }
        
        .results-btn.secondary {
            background: linear-gradient(to right, var(--accent), var(--accent-dark));
        }
        
        @media (max-width: 768px) {
            .results-container {
                padding: 20px;
                margin: 20px auto;
            }
            
            .results-title {
                font-size: 2rem;
            }
            
            .winner-name {
                font-size: 2rem;
            }
            
            .results-actions {
                flex-direction: column;
            }
            
            .results-table {
                font-size: 0.9rem;
            }
            
            .results-table th, 
            .results-table td {
                padding: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="game-container">
        <!-- شريط اللغات -->
        <div class="language-switcher">
            <button id="lang-ar" class="lang-btn active" data-lang="ar">العربية</button>
            <button id="lang-en" class="lang-btn" data-lang="en">English</button>
        </div>
        
        <!-- عنوان النتائج -->
        <header class="header">
            <div class="logo">
                <i class="fas fa-trophy"></i>
                <h1 data-i18n="resultsTitle">النتائج النهائية</h1>
            </div>
        </header>
        
        <!-- محتوى النتائج -->
        <div class="results-container">
            <!-- الفائز -->
            <div class="winner-section">
                <h2 data-i18n="congratulations">مبروك!</h2>
                <h3 data-i18n="winner">الفائز</h3>
                <div class="winner-name" id="winner-name">الفريق الأحمر</div>
                <div class="winner-score" id="winner-score">120 نقطة</div>
            </div>
            
            <!-- جميع النتائج -->
            <div class="all-results">
                <h3 data-i18n="finalScore">النتيجة النهائية</h3>
                <table class="results-table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th data-i18n="team">الفريق</th>
                            <th data-i18n="points">النقاط</th>
                        </tr>
                    </thead>
                    <tbody id="results-body">
                        <!-- سيتم ملؤها بواسطة JavaScript -->
                    </tbody>
                </table>
            </div>
            
            <!-- أزرار التحكم -->
            <div class="results-actions">
                <a href="game.html" class="results-btn">
                    <i class="fas fa-play-circle"></i>
                    <span data-i18n="playAgain">اللعب مرة أخرى</span>
                </a>
                <a href="index.html" class="results-btn secondary">
                    <i class="fas fa-home"></i>
                    <span>العودة للرئيسية</span>
                </a>
            </div>
        </div>
    </div>

    <script src="js/script.js"></script>
    <script>
        // عرض النتائج عند تحميل الصفحة
        document.addEventListener('DOMContentLoaded', function() {
            // جلب النتائج من التخزين المحلي
            const storedResults = localStorage.getItem('gameResults');
            let teams = [];
            
            if (storedResults) {
                teams = JSON.parse(storedResults);
            } else {
                // بيانات افتراضية إذا لم تكن هناك نتائج مخزنة
                teams = [
                    { id: 1, name: "الفريق الأحمر", color: "#ff6584", score: 120 },
                    { id: 2, name: "الفريق الأزرق", color: "#36d1dc", score: 90 },
                    { id: 3, name: "الفريق الأخضر", color: "#28a745", score: 70 },
                    { id: 4, name: "الفريق الأصفر", color: "#ffc107", score: 50 }
                ];
            }
            
            // ترتيب الفرق حسب النقاط (تنازليًا)
            teams.sort((a, b) => b.score - a.score);
            
            // عرض الفائز
            const winnerName = document.getElementById('winner-name');
            const winnerScore = document.getElementById('winner-score');
            
            if (winnerName && winnerScore && teams.length > 0) {
                winnerName.textContent = teams[0].name;
                winnerScore.textContent = `${teams[0].score} ${document.documentElement.lang === 'ar' ? 'نقطة' : 'points'}`;
                
                // تغيير لون القسم حسب لون الفريق الفائز
                const winnerSection = document.querySelector('.winner-section');
                if (winnerSection) {
                    winnerSection.style.background = `linear-gradient(to right, ${teams[0].color}33, ${teams[0].color}66)`;
                }
            }
            
            // عرض جميع النتائج في الجدول
            const resultsBody = document.getElementById('results-body');
            if (resultsBody) {
                resultsBody.innerHTML = '';
                
                teams.forEach((team, index) => {
                    const row = document.createElement('tr');
                    
                    // تحديد أيقونة المركز
                    let positionIcon = '';
                    if (index === 0) positionIcon = '🥇';
                    else if (index === 1) positionIcon = '🥈';
                    else if (index === 2) positionIcon = '🥉';
                    else positionIcon = `${index + 1}.`;
                    
                    row.innerHTML = `
                        <td class="position">${positionIcon}</td>
                        <td>
                            <div style="display: flex; align-items: center;">
                                <div style="width: 15px; height: 15px; border-radius: 50%; background-color: ${team.color}; margin-left: 10px;"></div>
                                ${team.name}
                            </div>
                        </td>
                        <td><strong>${team.score}</strong></td>
                    `;
                    
                    resultsBody.appendChild(row);
                });
            }
            
            // إضافة تأثيرات للجدول
            const tableRows = document.querySelectorAll('.results-table tbody tr');
            tableRows.forEach((row, index) => {
                // تأخير ظهور الصفوف تدريجيًا
                row.style.opacity = '0';
                row.style.transform = 'translateY(20px)';
                row.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                
                setTimeout(() => {
                    row.style.opacity = '1';
                    row.style.transform = 'translateY(0)';
                }, 100 * index);
            });
        });
    </script>
</body>
</html>